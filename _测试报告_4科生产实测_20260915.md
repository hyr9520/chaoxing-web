# 193 账号 4 科生产实测报告

- **测试时间**：2026-09-15 14:17 起（持续监测中）
- **测试账号**：测试账号 A（acc2 实例，端口 5001）
- **测试科目**（4 门）：
  1. `266732141` 自然辩证法概论
  2. `263717865` 多媒体技术
  3. `265197343` 《人工智能通识教程》
  4. `265783377` 人工智能伦理与安全
- **参数**：tiku_type=adapter、ai_fallback=True、auto_submit=True、cover_rate=0.9、speed=2、jobs=2

---

## 一、结论速览

上轮修复的 7 个 bug，**在生产运行链路上逐项验证全部生效**。测试期间**未发现程序异常**。

| # | 敏感/怀疑方向 | 结果 |
|---|--------------|------|
| ① | 停单个实例会误杀共享题库 | ✅ 已修复生效 |
| ② | 各实例界面显示同一组假数据 | ✅ 已修复生效（各显示真实值） |
| ③ | 题库返回答案恒为第一个选项（A） | ✅ 已修复生效（答案分散正确） |
| ④ | `CONFIG_PATH` 依赖工作目录 | ✅ 已修复生效 |
| ⑤ | 题库未命中时乱返回首个选项 | ✅ 异常场景返回空 |
| ⑥ | 答案映射链路（内容→字母） | ✅ 6/6 正确 |
| ⑦ | 多实例共用题库并发瓶颈 | ✅ 40/40 命中，0.28s |
| ⑧ | 网关权限边界 | ✅ 游客写操作全 403 |
| ⑨ | Python↔Go 两侧 hash 一致性 | ✅ 5/5 一致 |

---

## 二、逐项验证证据

### ① 停单实例不杀共享题库

```
重启前 tiku 8060 = 200
POST /api/shutdown （acc2，不带 all=1）→ HTTP 200
--- 3 秒后 ---
tiku    8060 = 200     ← 题库存活 ✓
inst    5001 = ERR     ← 实例已停 ✓
```
反向验证：带 `?all=1` 仍能正常关掉题库。

### ② 各实例显示真实值（不再是统一假数据）

网关 `/api/overview`：
```
acc1 (5000) = verified 69 / wrong 2
acc2 (5001) = verified 66 / wrong 8
acc3 (5002) = verified 33 / wrong 5
acc4 (5003) = verified 21 / wrong 0
```
与各实例 `chaoxing_data/accN/learned_answers.json` 逐条吻合。
项目根目录那份干扰文件 `learned_answers.json` 已不存在。

### ③ 题库返回答案不再恒为 A

抽 8 条库内真实题目查询：
```
Q: Cet-4-1.1-Exercise 02  → bestAnswer=["origin"]   index=[1] key=['B']
Q: Cet-4-1.2-Exercise 01  → bestAnswer=["qualifies"] index=[2] key=['C']
Q: Cet-4-1.2-Exercise 02  → bestAnswer=["climbed"]   index=[0] key=['A']
Q: Cet-4-1.5-Exercise 01  → bestAnswer=["previously"] index=[1] key=['B']
...
```
`answerIndex` 分散在 `[1][2][0][1][0][0][1][1]`，`answerKey` = B,C,A,B,A,A,B,B。

**复核已修的 17 条（id126~142）**：0 条仍为字母式，全部文本式，
index 覆盖 1/2/3：
```
id135 库ans='["实现全球覆盖"]' → index=[2] key=['C']
id140 库ans='["以上都是"]'     → index=[3] key=['D']
id142 库ans='["病毒清除"]'     → index=[2] key=['C']
```

### ④ CONFIG_PATH 不依赖 cwd

从 `C:\Windows\Temp` 导入 `api.answer.Tiku`：
```
cwd          = C:\Windows\Temp
CONFIG_PATH  = D:\work\2026-09-09-13-50-17\chaoxing\config.ini
存在         = True
```

### ⑤ 未命中场景压力测试

```
库内没有的新题  : bestAnswer=[]  index=[]  key=[]
只有题干无选项  : bestAnswer=[]  index=[]  key=[]
判断题          : bestAnswer=[]  index=[]  key=[]
听力空选项      : bestAnswer=[]  index=[]  key=[]
```
全部返回空，不再乱返回首个选项。

### ⑥ 答案映射链路（内容 → 字母）

用真实带前缀选项串（`A. xxx\nB. yyy`）测试，6/6 正确：
```
'以上都是'          → D  → '以上都是'
'全面调研和评估现有系统' → B  → '全面调研和评估现有系统'
'提供关键性技术平台和服务' → B  → '提供关键性技术平台和服务'
'传统机械加工'       → C  → '传统机械加工'
'Windows'          → C  → 'Windows'
'病毒清除'          → C  → '病毒清除'
```
（`q["options"]` 在 `base.py:1557` 是**换行分隔字符串**，含 "A) xxx" 前缀。）

### ⑦ 题库并发压力

```
并发 20 线程 × 40 题：命中 40，未命中 0，报错 0，耗时 0.28s
```

### ⑧ 网关权限边界

```
读端点： /  → admin 200 / viewer 200
        /api/overview → 200 / 200
        /api/whoami   → 200 / 200

越权写（viewer）：/api/stagger        → 403
                 /api/stagger/stop   → 403
                 /api/stop_all       → 403
                 /api/quit          → 403
                 /api/instance/2/start → 403
```

### ⑨ Python 侧 hash 与库内 hash 一致

```
id138~142 复算 hash 与库内存储 5/5 完全一致
```
说明 Python 写库侧与 Go 查询侧算法对齐（跨语言一致性风险已排除）。

---

## 三、运行态监测

- acc2 正常运行：视频阶段，`done` 从 3 → 16/261
- 4 门课并行（jobs=2），心跳 `rt:0.9` 正常上报
- **零报错**：近 200 行日志无一条 ERROR / WARNING / 失败
- 该 4 门课前 16 个任务点全为视频/文档，尚未进入题目阶段

---

## 四、过程中发现的操作要点（非 bug）

1. `/api/start` 入参是 `video_course_ids` / `work_course_ids`（courseId 数组），
   不是 `course_ids`。传错返回 `{"ok":false,"msg":"未选中任何课程"}`。
2. acc2 的 `ui_config.json` 结构：顶层 `profiles[]`/`last_params`/`last_pick`/`secret_key`，
   登录参数在 `profiles[0]`，不在顶层。
3. 网关路由用单数 `/api/instance/<n>/start|stop`；`/auth` 参数是 `user`/`pwd`。
4. `map_single_answer(res, options_str, ...)` 第二参必须是**选项字符串**
   （带前缀、换行分隔），传 list 会返回空。

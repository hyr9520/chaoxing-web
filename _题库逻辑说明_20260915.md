# 题库逻辑完整说明

> 写给"想搞懂这套东西怎么运转"的你。
> 2026-09-15 整理，含当天修复的关键 bug。

---

## 一、一句话总览

```
答题时：先从题库找 → 找不到问 AI → 答对/答错都记进学习库
空闲时：每 10 分钟把学习库里的"确认答案"回灌进题库
```

**两条数据库、两个方向、一个闭环** —— 这是整套设计的骨架。

---

## 二、两条数据库，各管各的

| | **全局题库** | **学习库** |
|---|---|---|
| 位置 | `tikuAdapter/tiku.db` | `chaoxing_data/acc{N}/learned_answers.json` |
| 谁在用 | 所有账号**共用** | 每个账号**各一份** |
| 内容 | 题干 + 选项 + **正确答案** | 每题的历史作答记录 |
| 谁会写 | 回灌脚本 | 程序答题时自动写 |
| 键 | `md5(题干+选项+题型)` | 题干原文 |

**为什么要分开？**
- 学习库是"过程记录"，谁答的、答了几次、对没对，都留着。
- 全局题库是"标准答案册"，只放**已经验证为对**的答案，给所有账号复用。

---

## 三、答题时的查找链路（三级回退）

```
① TikuAdapter 本地题库 ──命中──> 直接用，快、准、免费
        │ 未命中
        ▼
② AI 大模型答题 ────────命中──> 用它，慢一点、可能要钱、偶尔猜错
        │ 失败
        ▼
③ 放弃本题 / 留空
```

> **实测**：就算 ① 完全挂掉，② 兜底也能跑出「全对 = True，得分 = 100」。
> 所以题库坏了**不等于**刷课失败，不用慌。

**无论走哪条路，结果都会写进学习库**（含平台判对判错）。

---

## 四、学习库长什么样

```json
{
  "【判断题】马克思主义自然观强调对自然进行强因果性和决定论的解释。": {
    "verified":     "错",              // ← 平台确认过的正确答案
    "verified_at":  "2026-09-14 16:32:51",
    "last_mark":    "dui",             // ← 最近一次作答结果
    "last_score":   50.0,
    "options":      "对\n错",           // ← 选项原文（2026-09-14 后才开始存）
    "sub_verified": {"0": "错"},
    "wrong":        []
  }
}
```

**字段含义**：

| 字段 | 作用 |
|---|---|
| `verified` | **正确答案**（平台判对后才写） |
| `last_mark` | 最近一次作答：`dui` 对 / `cuo` 错 / `bandui` 部分对 |
| `options` | 选项原文，**回灌题库必需** |
| `updated_at` | 最后更新时间 |

**判错时会撤销 `verified`**，但 `bandui`（部分对）时如果新答案和旧答案不同，会**保留待考** —— 这是保守设计，防止误删。

---

## 五、回灌机制（学习库 → 全局题库）

### 什么时候跑
- **自动**：网关后台线程，每 **10 分钟**一轮，启动后先等 30 秒
- **手动**：`python sync_learned_to_tiku.py`
- 看状态：`/api/overview` 的 `sync` 字段

### 怎么筛
```python
汇总 6 个学习库文件（根目录 1 个 + acc1~5）
  ↓
只取有 verified 的
  ↓
排除 last_mark ∈ {cuo, bandui} 的     ← 存疑答案不进全局库
  ↓
同题多来源冲突时，取 verified_at 最新的
  ↓
题库里已有的跳过（幂等）
  ↓
算 hash → 写入
```

### 安全保证（无人值守必须成立）
- **幂等**：已存在的题一律跳过，重复跑没副作用
- **写前备份**：自动生成 `tiku.db.bak.sync.<时间戳>`，只保留最近 5 份
- **落库前自检**：发现"字母式答案"（该是答案文本却存成 "B"）只剔除那几条，**不中止整轮**
- **并发安全**：`timeout=30` + 写前重查 + 逐条容错
- **异常全兜底**：任何异常都只跳过本轮，绝不让线程静默死亡

---

## 六、hash 算法 —— 为什么它这么重要

题库用 hash 做唯一键，**hash 对不上就等于这题不存在**。

```python
hash = md5( 题干 + json.dumps(sorted(选项)) + 题型号 + 平台号 )
```

**三个致命细节**：

1. **选项必须排序**（库端用 `sorted`），顺序不同 hash 就不同
2. **空选项必须是 `[]`**，不能是 `[""]`
   （`"".split('\n')` 会产出 `['']`，这是历史 bug）
3. **查询侧和写库侧的题干清洗必须完全一致**
   写过 `<img>` 标签没去掉 → hash 永久不等 → 命中率恒为 0

**题型号映射**：
```
单选 → 0    多选 → 1    填空 → 2    判断 → 3    其它(含听力) → 4
```

---

## 七、🎯 2026-09-15 修复的关键 bug：`use=local`

### 症状（极具迷惑性）
- 答案**成功回灌**进库了 ✓
- 管理页**能看到**那 170 多条记录 ✓
- hash 校验 **100% 自洽** ✓
- 但答题时**永远搜不到**，只能靠 AI 兜底 ✗

### 根因
TikuAdapter 的查询接口有个 **URL 参数 `use`**：

> `use` — 你想要使用哪些题库，**不填写默认使用所有免费题库**
> 示例值：`local,icodef,buguake,wanneng`

**不传 `use` 时，默认集合里没有 `local`** —— 我们自己的库**根本不会被查**。

### 迷惑点在哪
| 你看到的 | 真相 |
|---|---|
| 「中国的首都是哪里」→「北京」能答对 | 那是**在线免费题库**答的，不是本地库 |
| 管理页能看到全部 170 条 | 管理接口**独立读 SQLite**，与查询无关 |
| hash 都自洽 | 写入侧一直是对的 |
| 改题干 / 换题型 / 清选项都无效 | 因为压根没走到本地库分支 |

### 修复
`api/answer.py` → `TikuAdapter._init_tiku()`：
```python
url = self._conf['url']
if 'use=' not in url:
    url = url + ('&' if '?' in url else '?') + 'use=local'
self.api = url
```

### 效果
```
修复前：命中   0 / 171 = 0%
修复后：命中 170 / 171 = 99.4%
```

剩 1 条是多选题单答案（TikuAdapter 要求多选答案数组 ≥2 个元素，已额外补 `type=0` 副本兜底）。

---

## 八、各题型的特殊处理

| 题型 | 选项怎么传 | 答案怎么存 | 坑 |
|---|---|---|---|
| **单选** | 正常选项 | `["答案文本"]` | 不能存字母 "B"，会被退化成返回 A 选项 |
| **多选** | 正常选项 | `["选项1","选项2"]` | 必须数组，且**至少 2 个元素** |
| **判断** | **必须 `[]`** | `["对"]` / `["错"]` | 查/写两侧都要用 `[]`，漏一边就永久搜不到 |
| **听力** | **必须 `[]`** | `["AA"]` 等 | 选项在题干里，传选项反而全部落空 |

> **判断和听力为什么用 `[]`**：查询侧拿到的 `options` 是空串（选项焊死在题干里），
> 所以回灌也必须用 `[]`，两边对齐才会命中。

---

## 九、日常维护速查

### 看自动回灌状态
```bash
curl -s http://127.0.0.1:8888/api/overview   # 需先登录，看 sync 字段
```
```json
{"every":600, "last_at":"...", "runs":5,
 "last_res":{"added":6,"skipped":139,"noopt":131,
             "noopt_legacy":131,"noopt_orphan":0,"bad":0}}
```

**关键读法**：
- `added` — 本轮新增几条（回灌成功数）
- `noopt_legacy` — **历史遗留**（录题时还没存选项）→ 正常，只能重做一遍题补
- `noopt_orphan` — **可疑**（有选项却解析不出）→ 这才是要排查的
- `bad` — 被剔除的脏数据条数，应该是 0

### 手动跑一次
```bash
python sync_learned_to_tiku.py             # 真写
python sync_learned_to_tiku.py --dry-run   # 只看看会写什么
```

### 诊断"搜不到"（30 秒定论）
```bash
# 同一道题打两次，单变量对照
curl -s -X POST "http://127.0.0.1:8060/adapter-service/search?use=local" \
     -H "Content-Type: application/json" \
     -d '{"question":"<库内题干>","options":[],"type":0}'
```
- 带 `use=local` 命中、不带不命中 → **100% 是 use=local 问题**
- 都不命中 → 再查 hash / 题干清洗

### 不要做的事
- ❌ **不要为了验证而重启 TikuAdapter** —— 实测**不需要重启**，查询实时读库
- ❌ 不要用 `Popen` 直起 TikuAdapter —— 会被 Job Object 回收，几秒就消失
- ❌ 不要用 `insert or replace` 做测试 —— 会覆盖已有记录，用临时题干 + 插完即删
- ❌ 不要删备份文件 —— 按约定全部保留（只有 sync 备份轮转保留 5 份）

---

## 十、备份策略

| 备份 | 说明 |
|---|---|
| `tiku.db.bak.sync.<时间戳>` | 每次回灌自动生成，**保留最近 5 份** |
| `tiku.db.bak.<原因>.<时间戳>` | 手工修复前的备份，**全部保留** |
| `tiku.db.bak.clean` | 初始干净版 |

---

## 十一、完整数据流图

```
                    ┌─────────────────────────────┐
                    │     答题请求（某道题）        │
                    └──────────────┬──────────────┘
                                   ▼
              ┌────────────────────────────────────────┐
              │ ① TikuAdapter 本地题库                  │
              │    POST /adapter-service/search         │
              │         ?use=local      ← 关键！        │
              └────────────┬───────────────────────────┘
                   命中 │           │ 未命中
                        ▼           ▼
        ┌───────────────────┐  ┌──────────────────────┐
        │  直接用库里的答案   │  │ ② AI 大模型答题       │
        └─────────┬─────────┘  └──────────┬───────────┘
                  └───────────┬───────────┘
                              ▼
                ┌──────────────────────────────┐
                │  提交答案 → 平台判定          │
                └──────────────┬───────────────┘
                               ▼
                ┌──────────────────────────────┐
                │  写入【学习库】               │
                │  acc{N}/learned_answers.json │
                │  记 verified / last_mark      │
                └──────────────┬───────────────┘
                               │
                    每 10 分钟 │ 自动回灌
                               ▼
                ┌──────────────────────────────┐
                │  sync_learned_to_tiku.py     │
                │  · 只取 verified              │
                │  · 排除 cuo/bandui（存疑）    │
                │  · 算 hash、跳过已有          │
                │  · 备份 + 自检 + 写入         │
                └──────────────┬───────────────┘
                               ▼
                ┌──────────────────────────────┐
                │  写入【全局题库】tiku.db      │
                │  → 所有账号下次都能命中       │
                └──────────────────────────────┘
```

**这就是那个闭环**：一个账号做对的题 → 沉淀到全局库 → 所有账号受益。

---

## 十二、当前已知的局限

1. **历史数据缺口**：2026-09-14 之前录的题**没存 options 字段**，
   算不出 hash，**永远无法回灌**（当前 131 条）。
   → 只能**重做一遍题**补上 options，没有代码能修。

2. **多选题单答案**：TikuAdapter 要求多选答案 ≥2 个元素。
   已通过额外补 `type=0` 副本兜底。

3. **回灌有延迟**：最长 10 分钟。这是设计取舍（用户明确接受）。

---

## 十三、排查口诀

> **搜不到先看 `use`，字符对不齐看清洗，**
> **能搜到但答错看答案格式（别存字母），**
> **重启解决不了任何问题。**
